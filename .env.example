# =============================================================================
# Personal Portfolio - Environment Variables Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit your .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# JWT Authentication (REQUIRED)
# -----------------------------------------------------------------------------
# Shared secret for JWT signing between auth-service and backend
# Generate with: openssl rand -base64 32
# MUST be at least 32 characters
AUTH_JWT_SECRET=your-jwt-secret-at-least-32-characters

# Better Auth secret for session encryption
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-better-auth-secret

# -----------------------------------------------------------------------------
# CORS Configuration (REQUIRED for production)
# -----------------------------------------------------------------------------
# Comma-separated list of allowed origins
# Development: http://localhost:3000
# Production: https://yourportfolio.com
CORS_ORIGINS=http://localhost:3000

# -----------------------------------------------------------------------------
# URL Configuration (REQUIRED for production)
# -----------------------------------------------------------------------------
# Public URLs for your services
# Development uses localhost defaults, production needs real domains
BETTER_AUTH_URL=http://localhost:3001
FRONTEND_URL=http://localhost:3000

# API URLs (used by frontend app)
# NEXT_PUBLIC_API_URL=http://localhost:8080
# NEXT_PUBLIC_AUTH_SERVICE_URL=http://localhost:3001

# -----------------------------------------------------------------------------
# Google OAuth (Optional)
# -----------------------------------------------------------------------------
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# File Storage - MinIO / DO Spaces (REQUIRED)
# -----------------------------------------------------------------------------
# Development: Use local MinIO (minio:9000)
# Production: Use DO Spaces (nyc3.digitaloceanspaces.com)
FILE_MINIO_ENDPOINT=minio:9000
FILE_MINIO_ACCESS_KEY=minioadmin
FILE_MINIO_SECRET_KEY=minioadmin
FILE_MINIO_SECURE=false
FILE_MINIO_BUCKET=portfolio-files

# -----------------------------------------------------------------------------
# pgAdmin - Development Only (Optional)
# -----------------------------------------------------------------------------
# Only needed if using pgAdmin for database management
# Start with: docker compose --profile pgadmin up -d
PGADMIN_DEFAULT_EMAIL=admin@portfolio.local
PGADMIN_DEFAULT_PASSWORD=your-pgadmin-password

# =============================================================================
# Production Checklist:
# =============================================================================
# [ ] Generate strong, unique secrets (AUTH_JWT_SECRET, BETTER_AUTH_SECRET)
# [ ] Set CORS_ORIGINS to your production domain
# [ ] Update all URLs to production domain
# [ ] Configure DO Spaces for file storage (FILE_MINIO_ENDPOINT=nyc3.digitaloceanspaces.com)
# [ ] Set FILE_MINIO_SECURE=true for production
# [ ] Rotate all secrets after initial deployment
# =============================================================================
